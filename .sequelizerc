const path = require("path");

module.exports = {
    "migrations-path": path.resolve(__dirname, "src", "database", "migrations"),
    "modelss-path": path.resolve(__dirname, "src", "api", "models")
};